{% extends 'base.html.twig' %}

{% block title %}Gestion des produits{% endblock %}

{% block body %}
<h1 class="text-center">Gestion des produits</h1>

<table class="table table-striped">
<thead>
<tr>
<th>Id</th>
<th>Titre</th>
<th>Description</th>
<th>Couleur</th>
<th>Taille</th>
<th>Collection</th>
<th>Photo</th>
<th>Prix</th>
<th>Stock</th>
<th>Date d'enregistrement</th>
<th>Actions</th>
</tr>
</thead>
<tbody>
{% for produit in produits %}
    
<tr>
<td>{{ produit.id }}</td>
<td>{{ produit.titre }}</td>
<td>{{ produit.description|slice(0, 30)  }}</td>
<td>{{ produit.couleur }}</td>
{# 
<td>{% for indice in produit.taille %}
{{ indice }}
<br>
{% endfor %}
</td> #}
<td>{{ produit.taille }}</td>
<td>{{ produit.collection }}</td>
<td><img src="{{ asset("img/upload/" ~ produit.photo) }}" alt="" width="50"></td>
<td>{{ produit.prix }} €</td>
<td>{{ produit.stock }}</td>
<td>{{ produit.dateEnregistrement|date('d/m/Y') }}</td>
<td>
<a href="{{ path('admin_modify', {id: produit.id}) }}" class="text-info me-2"><i class="bi bi-pencil-square"></i></a>
<a href="{{ path('admin_delete', {id: produit.id}) }}" class="text-danger"><i class="bi bi-trash3-fill"></i></a>
</td>
</tr>
{% endfor %}
</tbody>
</table>

{% endblock %}
